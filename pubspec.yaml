name: window_size_classes
description: "A Flutter package which provides window size classes as defined by Material Design 3."
version: 1.1.0+1
issue_tracker: https://github.com/shaakunuigurumi/window_size_classes.dart/issues
repository: https://github.com/shaakunuigurumi/window_size_classes.dart

screenshots:
  - description: "Example app showing the window size class."
    path: screenshots/example.webp

topics:
  - material
  - material3

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0